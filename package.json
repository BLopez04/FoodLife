{
  "name": "cs-307-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run -w express-backend",
    "dev": "npm run -w express-backend",
    "lint": "npx eslint . && npx prettier --check .",
    "test": "echo \"Error: no test specified\"",
    "format": "prettier --write './**/*.{js,jsx,ts,tsx,css,md,json}' --config ./.prettierrc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "http-server": "^14.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}
